services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: campusdesk
      POSTGRES_PASSWORD: campusdesk
      POSTGRES_DB: campusdesk
    ports:
      - "5432:5432"
    volumes:
      - campusdesk_pg:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U campusdesk -d campusdesk"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  campusdesk_pg:
